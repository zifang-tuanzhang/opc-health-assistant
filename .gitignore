# ============================================================
#  Git 映射规则：需要入库的一个不落，不该入库的绝不进历史
# ============================================================

# ── 虚拟环境（启动器自动创建，本机产物，绝不入库）──
.venv/
.venv*/
venv/
venv*/

# ── 密钥（正本存项目外 %USERPROFILE%\.opc_health\，此处双保险拦截任何形态）──
*.local.json
keys.local.json
keys.user.json
.env
.env.*
!.env.example
02_源码/deploy/.env
*keys.json
*_key*
*.pem
*.p12

# ── Python 缓存 / 测试产物 ──
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/

# ── 运行期日志（临时诊断，非交付物，绝不入库）──
*.log

# ── 编辑器 / 系统 ──
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── 交付构建产物（ZIP 由「打包交付.py」随时可再生，不入库）──
04_交付物/*.zip

# ── 内部工作档案（含未脱敏信息，非交付内容），一律不入库 ──
99_内部留档/
04_交付物/历史包归档/

# ── 项目管理留档（封板说明等状态记录，供本地查阅，不作为作品对外提交）──
封板说明_*.md

# ── 杂散临时文件 ──
*.bak
*.tmp
*.orig
_clean_*

# 注：wheels/ 目录【保留】——离线依赖闭环资产，必须随源码交付。
# 注：03_测试记录/ 下的 .md/.json/截图【保留】——可复核的交付证据。
