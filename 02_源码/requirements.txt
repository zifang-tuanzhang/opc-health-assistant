# OPC 接单吧·AI 医院资源查询与便民就医助手 · 后端依赖
# 安装：激活 venv 后 pip install -r requirements.txt
# 启动器默认优先从 wheels/ 离线安装（闭环，无需联网）。

fastapi>=0.110,<1.0   # Web 框架：API 网关与 /chat、/health 端点
uvicorn[standard]     # ASGI 服务器：本地启动与容器运行
pydantic>=2.0         # 请求/响应模型：4 段式输出契约（见 src/schema.py）
httpx>=0.27           # HTTP 客户端：调用搜索 / 模型 API
python-dotenv>=1.0    # .env 加载：密钥服务端环境变量化（绝不进前端/仓库/日志）
openai>=1.40          # 模型客户端：所有国内厂商均为 OpenAI 兼容网关，一套 SDK 通用
pytest>=8.0           # 8 组测试与红线自动化
