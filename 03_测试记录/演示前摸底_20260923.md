=== 18:01 实网摸底快照（供演示前对照） ===
# 演示前实网摸底（2026-09-23 18:01 快照）

> 目的：验证员 P4 提醒「搜索引擎风控是时段性的，演示前摸一次底」。此为当晚 18:01 的实测时刻快照。
> 探针方式：绕过测试套，直调项目真实模块 `llm_client.chat` 与 `search.web_search`（与线上链路同一函数）。

| 探针 | 结果 |
|---|---|
| 激活供应商 | deepseek（`~/.opc_health/keys.json`） |
| LLM 直调（"只回复两个字：正常"） | ✅ 0.9 秒返回，模型响应正常 |
| 检索直调（"昆明 三甲医院 心血管内科"，四后端） | ✅ 7.0 秒命中 5 条，含夸克/神马真实医院页（URL 可点） |

**结论：18:01 时刻实网链路全活。** 此条不构成答辩现场的保证——现场前仍请按 README 跑一次 `python 跑全部测试.py`（约 5 分钟，十二套 = 离线九套 + 实网三套）复确认。

## 附：旧交付包归档（防拿错）

0921、0922 等旧包已移入 `04_交付物/历史包归档/`，`04_交付物/` 根目录只保留**唯一一份终版包**（当前为 `OPC接单吧第三届_交付包_20260924.zip`）。

> 注：交付包完整性校验值（SHA-256）随每次内容变动而改变，**不在包内文档中固化**（否则文档一变哈希即失效）。提交时以复验脚本 `复验交付包.py` 即时计算、并在交付通知中单独给出的哈希为准。
